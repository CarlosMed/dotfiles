1. Navigate to `about:config` and enable `toolkit.legacyUserProfileCustomizations.stylesheets`
1. Navigate to `about:profiles` and click on `Show in finder` for the root
   directory
1. Copy Chrome into that root folder
